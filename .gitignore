def div(num1, num2):
    if num1 == 0:  # Check if num1 is zero to avoid ZeroDivisionError
        raise ZeroDivisionError("Cannot divide by zero.")
    return num2 / num1

result = div(num1, num2)
print('Divide =', result)  # Place the variables outside of the string for proper formatting
